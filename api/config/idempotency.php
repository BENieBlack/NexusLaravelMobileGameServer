<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Idempotency Cache TTL (Time To Live)
    |--------------------------------------------------------------------------
    |
    | X-Unique-Request-Identifierを使った冪等性保証のためのキャッシュ期間（秒）
    | デフォルト: 86400秒（24時間）
    |
    | 環境変数 IDEMPOTENCY_CACHE_TTL で上書き可能
    |
    | キャッシュストア: Redis（CACHE_STORE=redis）
    |
    */
    'cache_ttl' => env('IDEMPOTENCY_CACHE_TTL', 86400), // 24時間

    /*
    |--------------------------------------------------------------------------
    | Idempotency Cache Key Prefix
    |--------------------------------------------------------------------------
    |
    | キャッシュキーのプレフィックス
    |
    */
    'cache_prefix' => 'idempotency',

    /*
    |--------------------------------------------------------------------------
    | Idempotency Enabled
    |--------------------------------------------------------------------------
    |
    | 冪等性機能の有効/無効
    | 開発環境では無効にすることも可能
    |
    */
    'enabled' => env('IDEMPOTENCY_ENABLED', true),
];
