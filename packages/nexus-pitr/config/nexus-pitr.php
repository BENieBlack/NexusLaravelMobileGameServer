<?php

return [
    /*
    |--------------------------------------------------------------------------
    | PITR Batch Size
    |--------------------------------------------------------------------------
    |
    | LogDB書き込み時のバッチサイズ
    | 大量のレコードを扱う場合、この値を調整してパフォーマンスを最適化
    |
    */
    'batch_size' => env('PITR_BATCH_SIZE', 1000),

    /*
    |--------------------------------------------------------------------------
    | Enable Compression
    |--------------------------------------------------------------------------
    |
    | before_data/after_dataフィールドをgzip圧縮するかどうか
    | ストレージを節約できるが、CPUコストが増加
    |
    */
    'enable_compression' => env('PITR_ENABLE_COMPRESSION', false),

    /*
    |--------------------------------------------------------------------------
    | Active TrxDB Connections
    |--------------------------------------------------------------------------
    |
    | PITRで使用するTrxDB接続のリスト
    | カンマ区切りで指定（例: trx1,trx2）
    |
    */
    'active_trx_connections' => explode(',', env('PITR_ACTIVE_TRX_CONNECTIONS', 'trx')),

    /*
    |--------------------------------------------------------------------------
    | Recovery Options
    |--------------------------------------------------------------------------
    |
    | 復旧コマンドのデフォルト設定
    |
    */
    'recovery' => [
        'dry_run' => env('PITR_RECOVERY_DRY_RUN', true), // デフォルトはドライラン
        'verify' => env('PITR_RECOVERY_VERIFY', true),   // 復旧後に検証を実行
        'batch_size' => env('PITR_RECOVERY_BATCH_SIZE', 100), // 復旧時のバッチサイズ
    ],

    /*
    |--------------------------------------------------------------------------
    | Verification Options
    |--------------------------------------------------------------------------
    |
    | 整合性検証のデフォルト設定
    |
    */
    'verification' => [
        'sample_rate' => env('PITR_VERIFY_SAMPLE_RATE', 0.1), // サンプリング率（10%）
        'max_records' => env('PITR_VERIFY_MAX_RECORDS', 10000), // 最大検証レコード数
    ],
];
