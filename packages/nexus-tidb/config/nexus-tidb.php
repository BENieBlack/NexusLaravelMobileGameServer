<?php

return [
    /*
    |--------------------------------------------------------------------------
    | TiDBとして扱うか
    |--------------------------------------------------------------------------
    |
    | trueの場合、trx/log の単一主キー id を AUTO_INCREMENT ではなくUUIDで払い出す。
    |
    | TiDBは分散DBのため AUTO_INCREMENT が単調増加を保証せず、
    | 連番キーは書き込みが特定リージョンへ集中する原因にもなる。
    | ランダムなUUIDにすることで書き込みを分散させる。
    |
    | env() を直接読むと config:cache 済みの本番で null になるため、
    | 参照は必ずこの config を経由する。
    |
    */
    'is_tidb' => env('DB_IS_TIDB', false),
];
