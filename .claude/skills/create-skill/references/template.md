# SKILL.md 雛形

`.claude/skills/<skill-name>/SKILL.md` として保存する。`<...>` を埋め、不要な節は削る。

---

```markdown
---
name: <skill-name>
description: <何をするか（1文）>。<どう判断・処理するか（1〜2文）>。「<トリガー語1>」「<トリガー語2>」「<トリガー語3>」などで発動する。
---

# <スキル名>

## 使う場面

<いつ使うか。誤爆しやすいなら「〜のときは使わない」も書く>

## 手順

1. <最初にやること>
2. <次にやること。分岐があるなら条件を明示する>
3. <確認方法>

## 判断基準

| 条件 | 対応 |
|---|---|
| <条件A> | <対応A> |
| <条件B> | <対応B> |

## 落とし穴

- **<やりがちな誤り>** — <なぜダメか。1行で>
- **<やりがちな誤り>** — <なぜダメか。1行で>

## 参照

- <関連ドキュメントへのリンク>
```

---

## 埋めるときの注意

### description

以下を全部含める。

- **何をするか** — 動詞で始める（「〜を作成する」「〜を検証する」）
- **いつ発動するか** — ユーザーが実際に打つ言葉を3つ以上並べる
- **範囲外** — 誤爆しやすいスキルなら「〜のときは使わない」を1文

セッション開始時に読まれるのは description だけ。ここが弱いと本文は一生読まれない。

### 手順

- 命令形で書く
- 分岐は条件を明示する（「Aなら〜、Bなら〜」）
- 各手順に理由を1行添える。理由があれば状況が変わっても応用できる

### 落とし穴

**このリポジトリで実際に踏んだ失敗だけを書く。** 一般論を書くと、本当に必要な情報が埋もれる。

コミット履歴の `fix:` を検索すると、実際に踏んだ失敗が見つかる。

```bash
git log --oneline --grep="^fix:" | head -30
```

## 記入例

```markdown
---
name: trx-migration
description: trx（トランザクション）系のマイグレーションを作成・修正するときに使う。シャード全台に適用する
  $connections の書き方と、--path の渡し方の落とし穴を案内する。「trxのマイグレーションを作って」
  「シャードにテーブルを追加」「マイグレーションが片方のシャードにしか流れない」などで発動する。
  mst/sys/log のマイグレーションには使わない。
---

# trx系マイグレーションの作成

## 使う場面

`api/database/migrations/trx/` 配下のマイグレーションを触るとき。
mst/sys/log は単一DBなのでこのスキルは不要。

## 手順

1. `api/database/migrations/trx/` に `YYYY_MM_DD_NNNNNN_<説明>.php` を作る
2. クラスに `$connections = ['trx1', 'trx2']` を必ず書く
3. `DB_TRX_SHARDS` を確認する。シャードを増やしている場合は配列も合わせる
4. 実行して両シャードに反映されたか確認する

## 落とし穴

- **`$connections` の書き忘れ** — 既定接続にしか流れず、trx2だけスキーマが古くなる。
  不整合は運用中に初めて表面化するので気付くのが遅れる
- **`--path` に絶対パスを渡す** — `--realpath` を付けない限り `base_path()` からの相対解決になり、
  存在しないパス扱いで**エラーも出さずに無視される**（コミット `7f4c1db`）
```
